do
{
     visualThinking.Draw();
     workTask.Excute();
     if (designGood.iterate())
{
     if (designBoard.Draw(())
         designBoard.Storytelling();
    designGood.Code();
    }
}   while (!workTask.Done())

while (!workTask.Done()) {
  if (designGood.iterate()) {
    visualThinking.Draw();
    workTask.Excute();
  } else {
    designBoard.Draw();
    designBoard.Storytelling();
    designGood.Code();
  }
}
